# shellnotes

Remeber, like everything in linux in a file, everything in shell is string.

check the variable in shell

- using set(help set)
- using command env or export -p(help export)
- bash 4 or above using declare -p(help declare)
